export default function typography(theme) {
  return {
    fontFamily: theme.palette.font.main,
    fontWeightLight: 300,
    fontWeightRegular: 400,
    fontWeightMedium: 500,
    ffontWeightSemiBold: 600,
    fontWeightBold: 700,
    h1: {
      fontSize: "60px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h2: {
      fontSize: "48px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h3: {
      fontSize: "40px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h4: {
      fontSize: "30px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h5: {
      fontSize: "24px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h6: {
      fontSize: "22px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    h7: {
      fontSize: "20px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    body1: {
      fontSize: "16px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },
    body2: {
      fontSize: "14px",
      fontFamily: theme.palette.font.main,
      fontWeight: "normal",
    },

    f46: {
      fontSize: "46px",
      fontFamily: theme.palette.font.main,
    },

    f44: {
      fontSize: "44px",
      fontFamily: theme.palette.font.main,
    },

    f36: {
      fontSize: "38px",
      fontFamily: theme.palette.font.main,
    },

    f36: {
      fontSize: "36px",
      fontFamily: theme.palette.font.main,
    },
    f34: {
      fontSize: "34px",
      fontFamily: theme.palette.font.main,
    },
    f32: {
      fontSize: "32px",
      fontFamily: theme.palette.font.main,
    },

    f26: {
      fontSize: "26px",
      fontFamily: theme.palette.font.main,
    },

    f22: {
      fontSize: "22px",
      fontFamily: theme.palette.font.main,
    },

    f18: {
      fontSize: "18px",
      fontFamily: theme.palette.font.main,
    },

    f10: {
      fontSize: "10px",
      fontFamily: theme.palette.font.main,
    },
    f8: {
      fontSize: "8px",
      fontFamily: theme.palette.font.main,
    },
  };
}
