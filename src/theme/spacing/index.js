// this will be a function and it will return a no or obect so you can use differen spacing

// const theme = createMuiTheme({
//     spacing: 8,
//   });
// When your theme is define, you have a spacing value.
// By default this value is 8px. So when you call theme.spacing(20),
// if the spacing value is 8 it return simply 20 * 8 => 160px.

export default function spacing(theme) {
  return 5;
}
